[pytest]
testpaths = tests
addopts = -v --tb=short --cov=src --cov-report=term-missing
markers =
    slow: marks tests as slow (deselect with '-m "not slow"')
    integration: marks tests as integration tests
    simulation: marks tests as device simulation tests
